#include <iostream>

#include "Box.h"

std::vector<Point> Box::createBoxPoints(int length, int divisions) {
    std::vector<Point> ans = std::vector<Point>();

    ans.emplace_back(1, 2, 3);

    return ans;
}
