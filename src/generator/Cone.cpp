#include <iostream>

#include "Cone.h"

std::vector<Point> Cone::createConePoints(int radius, int height, int slices, int stacks) {
    std::vector<Point> ans = std::vector<Point>();

    ans.emplace_back(4, 5, 6);

    return ans;
}
