#include <iostream>

#include "Cone.h"

std::vector<Vertex> Cone::createConePoints(int radius, int height, int slices, int stacks) {
    std::vector<Vertex> ans = std::vector<Vertex>();

    ans.emplace_back(4, 5, 6);

    return ans;
}
